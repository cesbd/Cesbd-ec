

<header id="landing-header" class="flex items-center py-4 px-8 bg-transparent sticky z-10 xl:flex-row flex-col top-0 left-0">
  <div class="bg-amber-300 p-4 polygon px-20">
    <a href="#" class="text-xl font-semibold text-black lg:ml-16 inline-block lg:text-start">
      <span class="font-bold text-3xl inline-block lg:-ml-20">Black & Decker</span>
      <br />Innovación y Accesibilidad al Alcance de Todos
    </a>
  </div>

  <nav>
    <ul class="flex [&>li>a]:inline-block [&>li>a]:px-4 [&>li>a]:py-2 [&>li>a]:font-semibold [&>li>a]:text-white [&>li>a]:text-lg">
      <li><a class="hover:text-black hover:transition-colors" href="#">Inicio</a></li>
      <li><a class="hover:text-black hover:transition-colors" href="#">Marcas</a></li>
      <li><a class="hover:text-black hover:transition-colors" href="#">Eventos</a></li>
      <li><a class="hover:text-black hover:transition-colors" href="#">Promociones</a></li>
      <li><a class="hover:text-black hover:transition-colors" href="#">Capacitaciones</a></li>
      <li><a class="hover:text-black hover:transition-colors" href="#">Contacto</a></li>
    </ul>
  </nav>

  <div
    id="menu-backdrop"
    class={`
      absolute left-0 top-0 
      bg-amber-200 backdrop:blur-lg rounded
      translate-x-[var(--menuLeft)] translate-y-[var(--menuTop)]
      w-[var(--menuWidth)] h-[var(--menuHeight)]
      transition-all duration-300 ease-in-out opacity-0 -z-10
    `}
  >
  </div>
</header>

<script>
  const menuBackdrop = document.getElementById("menu-backdrop") as HTMLElement;
  const listItems = document.querySelectorAll(
    "#landing-header li",
  ) as NodeListOf<HTMLElement>;

  listItems.forEach((item) => {
    item.addEventListener("mouseenter", () => {
      const { left, top, width, height } = item.getBoundingClientRect();
      menuBackdrop.style.setProperty("--menuLeft", `${left}px`);
      menuBackdrop.style.setProperty("--menuTop", `${top}px`);
      menuBackdrop.style.setProperty("--menuWidth", `${width}px`);
      menuBackdrop.style.setProperty("--menuHeight", `${height}px`);
      menuBackdrop.style.opacity = "1";
      menuBackdrop.style.visibility = "visible";
    });

    item.addEventListener("mouseleave", (e) => {
      menuBackdrop.style.opacity = "0";
      menuBackdrop.style.visibility = "hidden";
    });
  });
</script>
